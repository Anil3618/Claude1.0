# Copy to .env.local in the web/ directory for local development

# Optional: API key for B2B endpoint protection.
# Leave blank to allow unauthenticated access (fine for consumer MVP).
# Set in Vercel dashboard under Environment Variables when you add B2B customers.
API_KEY_FREE=your_free_tier_api_key_here
